$(document).ready(function(){
  $('#ascensor').ascensor({
    childType: "section",
    time: 500,
    windowsOn: 0,
    direction: 'x',
    easing: "easeInOutQuart"
  });
  // var ascensorInstance = ascensor.data('ascensor');
});
